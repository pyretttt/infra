apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: ingress
  namespace: flux-system
spec:
  interval: 1m
  path: ./manifests/ingress
  prune: true
  dependsOn:
    - name: apps
    - name: ingress-nginx
  sourceRef:
    kind: GitRepository
    name: flux-system
