- name: Install and enable chrony
  hosts: vps
  become: true
  tasks:
    - name: Install chrony
      apt:
        name: chrony
        state: present

    - name: Ensure chrony is running
      systemd:
        name: chrony
        state: started
        enabled: yes
