apiVersion: v1
kind: Service
metadata:
  name: wireguard
  namespace: default
spec:
  type: NodePort
  selector:
    name: wireguard
  ports:
    - name: wg
      port: 51820
      targetPort: 51820
      nodePort: 31602
      protocol: UDP
