apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-static-config-1
data:
  default.conf: |
    server {
        listen 80;
        location / {
            return 200 'Hello, this is static text from Nginx #1!';
            add_header Content-Type text/plain;
        }
    }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: static-nginx-deployment-1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: static-nginx-1
  template:
    metadata:
      labels:
        app: static-nginx-1
    spec:
      containers:
      - name: nginx
        image: nginx:alpine
        ports:
        - containerPort: 80
        volumeMounts:
        - name: config-volume
          mountPath: /etc/nginx/conf.d/default.conf
          subPath: default.conf
      volumes:
      - name: config-volume
        configMap:
          name: nginx-static-config-1
---
apiVersion: v1
kind: Service
metadata:
  name: static-nginx-service-1
spec:
  type: ClusterIP
  selector:
    app: static-nginx-1
  ports:
  - port: 80
    targetPort: 80
