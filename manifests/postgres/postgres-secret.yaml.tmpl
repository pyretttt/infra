apiVersion: v1
kind: Secret
metadata:
  name: postgres-auth
  namespace: default
type: Opaque
stringData:
  # App role (not the postgres superuser). Generate once and keep stable across reinstalls.
  username: ${POSTGRES_USER}
  # Password for the postgres superuser. Generate once and keep stable across reinstalls:
  #   openssl rand -base64 16
  postgres-password: ${POSTGRES_USER_PASSWORD}
  admin-password: ${POSTGRES_ADMIN_PASSWORD}
